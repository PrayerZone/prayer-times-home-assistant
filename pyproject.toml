[project]
name = "prayer-times-home-assistant"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["homeassistant>=2024.1.0"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
